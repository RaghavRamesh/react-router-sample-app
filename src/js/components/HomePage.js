import React from 'react';
import SimpleSliderContainer from '../containers/SimpleSliderContainer';

/**
 * Represents the HomePage React Component which is responsible for rendering the
 * SimpleSliderContainer component.
 */
export default class HomePage extends React.Component {
	render(props) {
		return <SimpleSliderContainer></SimpleSliderContainer>;
	}
}
